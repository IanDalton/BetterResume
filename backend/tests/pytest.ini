[pytest]
testpaths = tests
asyncio_mode = auto
timeout = 60
timeout_func_only = true
markers =
    real_ai: requires live API keys (skip by default)
    slow: multi-model comparison, takes several minutes
