// Shared app-wide constants that more than one component needs, so a future
// change (e.g. a rotated affiliate link) only has one call site to update.

/** Direct MercadoPago checkout link, used for donors detected as being in Argentina. */
export const MERCADOPAGO_URL = 'https://link.mercadopago.com.ar/betterresume';
