name: Build and publish Docker image to GHCR

on:
    push:
        branches:
            - main
        paths:
          - 'backend/**'
          - '.github/workflows/upload-image.yml'

permissions:
    contents: read
    packages: write # needed so GITHUB_TOKEN can push to ghcr.io

jobs:
    build-and-push:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Lowercase Repo Owner
              run: |
                  echo "REPO_OWNER=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata (tags, labels)
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ghcr.io/${{ env.REPO_OWNER }}/better-resume

            - name: Build and push Docker image
              uses: docker/build-push-action@v6
              with:
                  context: backend/
                  push: true
                  platforms: linux/amd64
                  tags: |
                      ghcr.io/${{ env.REPO_OWNER }}/better-resume:latest
                      ghcr.io/${{ env.REPO_OWNER }}/better-resume:${{ github.sha }}